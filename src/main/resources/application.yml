server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/eshop
    username: eshop
    password: pa55w0rd
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: on

